const clamp=(v,min=0,max=100)=>Math.max(min,Math.min(max,v));
export class DerivedIndicatorEngine {
  constructor(){this.byCountry=new Map();}
  update(states,{energy,economy,military}={}){this.byCountry.clear();for(const state of states.values()){const e=energy?.get(state.code)||{},x=economy?.get(state.code)||{},m=military?.get(state.code)||{};const indicators={logisticsCapacity:clamp(x.logisticsCapacity??50),energySecurity:clamp(e.energySecurity??state.metrics.energy.current),industrialCapacity:clamp(x.industrialOutput??state.metrics.infrastructure.current),digitalConnectivity:clamp(x.digitalConnectivity??state.metrics.technology.current),resilience:clamp(x.resilience??state.metrics.resilience.current),tradeDependency:clamp(x.tradeDependency??50),militarySustainment:clamp(m.readiness??state.metrics.militaryReadiness.current)};indicators.explanations={energySecurity:[{label:'Domestic generation',value:Math.round((e.energyAvailability??50)*.42)},{label:'Grid redundancy',value:Math.round((e.gridResilience??50)*.28)},{label:'Storage',value:Math.round((e.storage??50)*.18)},{label:'Import dependency',value:-Math.round((e.energyDependency??50)*.12)}],logisticsCapacity:[{label:'Route availability',value:Math.round(indicators.logisticsCapacity*.55)},{label:'Infrastructure health',value:Math.round(state.metrics.infrastructure.current*.25)},{label:'Trade dependency',value:-Math.round(indicators.tradeDependency*.2)}]};const row={...indicators,dataClass:'DERIVED_TRANSPARENT'};this.byCountry.set(state.code,row);state.derivedIndicatorsV2=row;}return this.byCountry;}
  get(code){return this.byCountry.get(code)||null;}
}
